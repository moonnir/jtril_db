comment on column orbrgs.nmdtlsls_log_mig.apprdate is
    'This column is use for the approval date of transaction';

comment on column orbrgs.nmdtlsls_log_mig.apprqnty is
    'This column is use for the approval quantity of product';

comment on column orbrgs.nmdtlsls_log_mig.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.nmdtlsls_log_mig.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.nmdtlsls_log_mig.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.nmdtlsls_log_mig.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.nmdtlsls_log_mig.itemcode is
    'This column is hold Item Code define for the each product.';

comment on column orbrgs.nmdtlsls_log_mig.orgnqnty is
    'This column is use for the quantity of product';

comment on column orbrgs.nmdtlsls_log_mig.partycde is
    'This column is use for the supplier/Customer/Party Code for the transaction';

comment on column orbrgs.nmdtlsls_log_mig.raisedby is
    'This column is use for hold the information who is raise the document.';

comment on column orbrgs.nmdtlsls_log_mig.reqddate is
    'This column is use for the request date of the transaction.';

comment on column orbrgs.nmdtlsls_log_mig.serlnumb is
    'This column is use for the Serial number for each transaction';

comment on column orbrgs.nmdtlsls_log_mig.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"c7df54b07cc7c4bd6bf365c643129edf92e75555","type":"COMMENT","name":"nmdtlsls_log_mig","schemaName":"orbrgs","sxml":""}