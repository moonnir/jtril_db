comment on column orbrgs.nmmsserv_log.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.nmmsserv_log.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.nmmsserv_log.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.nmmsserv_log.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.nmmsserv_log.itemcode is
    'This column is hold Item Code define for the each product.';

comment on column orbrgs.nmmsserv_log.partycde is
    'This column is use for the supplier/Customer/Party Code for the transaction';

comment on column orbrgs.nmmsserv_log.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"789dc205ac924e22f4e96d059dfbe6e6f073dee0","type":"COMMENT","name":"nmmsserv_log","schemaName":"orbrgs","sxml":""}