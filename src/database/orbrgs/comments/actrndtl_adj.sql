comment on column orbrgs.actrndtl_adj.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.actrndtl_adj.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.actrndtl_adj.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.actrndtl_adj.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.actrndtl_adj.itemcode is
    'This column is hold Item Code define for the each product.';

comment on column orbrgs.actrndtl_adj.serlnumb is
    'This column is use for the Serial number for each transaction';

comment on column orbrgs.actrndtl_adj.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"3b2d667f0e88acd3efacfd80e25b5f9e9071740d","type":"COMMENT","name":"actrndtl_adj","schemaName":"orbrgs","sxml":""}