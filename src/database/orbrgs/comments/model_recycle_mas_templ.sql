comment on column orbrgs.model_recycle_mas_templ.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.model_recycle_mas_templ.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.model_recycle_mas_templ.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.model_recycle_mas_templ.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.model_recycle_mas_templ.itemcode is
    'This column is hold Item Code define for the each product.';

comment on column orbrgs.model_recycle_mas_templ.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"51b667fa7b8dc6caefd2f2da4df4e47a32e37266","type":"COMMENT","name":"model_recycle_mas_templ","schemaName":"orbrgs","sxml":""}