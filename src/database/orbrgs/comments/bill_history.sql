comment on column orbrgs.bill_history.apprdate is
    'This column is use for the approval date of transaction';

comment on column orbrgs.bill_history.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.bill_history.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.bill_history.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.bill_history.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.bill_history.raisedby is
    'This column is use for hold the information who is raise the document.';

comment on column orbrgs.bill_history.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"20c0b943fea2e9ef38fe0e99015866038d1a3dce","type":"COMMENT","name":"bill_history","schemaName":"orbrgs","sxml":""}