comment on column orbrgs.actrn_srv.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.actrn_srv.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.actrn_srv.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.actrn_srv.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.actrn_srv.itemcode is
    'This column is hold Item Code define for the each product.';

comment on column orbrgs.actrn_srv.serlnumb is
    'This column is use for the Serial number for each transaction';

comment on column orbrgs.actrn_srv.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"176754793610b65692413a5a3b078d0ea75e4659","type":"COMMENT","name":"actrn_srv","schemaName":"orbrgs","sxml":""}