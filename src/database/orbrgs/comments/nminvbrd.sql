comment on column orbrgs.nminvbrd.apprdate is
    'This column is use for the approval date of transaction';

comment on column orbrgs.nminvbrd.apprqnty is
    'This column is use for the approval quantity of product';

comment on column orbrgs.nminvbrd.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.nminvbrd.compltfg is
    'Process complete flag. 0=Process Pending 1=Process Complete.';

comment on column orbrgs.nminvbrd.damagqty is
    'Damage or Reject Qty';

comment on column orbrgs.nminvbrd.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.nminvbrd.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.nminvbrd.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.nminvbrd.itemcode is
    'This column is hold Item Code define for the each product.';

comment on column orbrgs.nminvbrd.itmstats is
    'Item wise status';

comment on column orbrgs.nminvbrd.orgnqnty is
    'This column is use for the quantity of product';

comment on column orbrgs.nminvbrd.partycde is
    'This column is use for the supplier/Customer/Party Code for the transaction';

comment on column orbrgs.nminvbrd.raisedby is
    'This column is use for hold the information who is raise the document.';

comment on column orbrgs.nminvbrd.reqddate is
    'This column is use for the request date of the transaction.';

comment on column orbrgs.nminvbrd.serlnumb is
    'This column is use for the Serial number for each transaction';

comment on column orbrgs.nminvbrd.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"8ddd043c3a7300e0533a9d3737406a9960bf1476","type":"COMMENT","name":"nminvbrd","schemaName":"orbrgs","sxml":""}