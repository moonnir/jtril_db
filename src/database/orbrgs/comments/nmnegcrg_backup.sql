comment on column orbrgs.nmnegcrg_backup.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.nmnegcrg_backup.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.nmnegcrg_backup.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.nmnegcrg_backup.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.nmnegcrg_backup.partycde is
    'This column is use for the supplier/Customer/Party Code for the transaction';

comment on column orbrgs.nmnegcrg_backup.serlnumb is
    'This column is use for the Serial number for each transaction';

comment on column orbrgs.nmnegcrg_backup.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"324bd5c0d10495ad18dac8782c97c5118808e6b0","type":"COMMENT","name":"nmnegcrg_backup","schemaName":"orbrgs","sxml":""}