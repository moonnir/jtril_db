comment on column orbrgs.appointment_detail.apprdate is
    'This column is use for the approval date of transaction';

comment on column orbrgs.appointment_detail.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.appointment_detail.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.appointment_detail.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.appointment_detail.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.appointment_detail.partycde is
    'This column is use for the supplier/Customer/Party Code for the transaction';

comment on column orbrgs.appointment_detail.raisedby is
    'This column is use for hold the information who is raise the document.';

comment on column orbrgs.appointment_detail.serlnumb is
    'This column is use for the Serial number for each transaction';

comment on column orbrgs.appointment_detail.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"9dfc467f37a67721cf12c06615187ded7401e292","type":"COMMENT","name":"appointment_detail","schemaName":"orbrgs","sxml":""}