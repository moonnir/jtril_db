comment on column orbrgs.actrndtl.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.actrndtl.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.actrndtl.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.actrndtl.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.actrndtl.itemcode is
    'This column is hold Item Code define for the each product.';

comment on column orbrgs.actrndtl.payment_receipt_type is
    'payment and receipt type';

comment on column orbrgs.actrndtl.serlnumb is
    'This column is use for the Serial number for each transaction';

comment on column orbrgs.actrndtl.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"77ed428afd8177b4dfece18b32abe9fedebf67ca","type":"COMMENT","name":"actrndtl","schemaName":"orbrgs","sxml":""}