comment on column orbrgs.accheque.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.accheque.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.accheque.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.accheque.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.accheque.serlnumb is
    'This column is use for the Serial number for each transaction';

comment on column orbrgs.accheque.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"bf88299d9f8008744caf4d64633cb471f99e6290","type":"COMMENT","name":"accheque","schemaName":"orbrgs","sxml":""}