comment on column orbrgs.mv_sttrndtl.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.mv_sttrndtl.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.mv_sttrndtl.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.mv_sttrndtl.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.mv_sttrndtl.serlnumb is
    'This column is use for the Serial number for each transaction';

comment on column orbrgs.mv_sttrndtl.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"35936001be1c2fc8328c1a280de8036151a1c0a5","type":"COMMENT","name":"mv_sttrndtl","schemaName":"orbrgs","sxml":""}