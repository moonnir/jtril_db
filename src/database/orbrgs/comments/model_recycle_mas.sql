comment on column orbrgs.model_recycle_mas.compcode is
    'This column is use for the hold company code';

comment on column orbrgs.model_recycle_mas.docnumbr is
    'This column is use for the hold system generated number for each transaction';

comment on column orbrgs.model_recycle_mas.doctdate is
    'This column is use for the hold transaction date for each transaction';

comment on column orbrgs.model_recycle_mas.docttype is
    'This column is use for the document type for the transaction';

comment on column orbrgs.model_recycle_mas.itemcode is
    'This column is hold Item Code define for the each product.';

comment on column orbrgs.model_recycle_mas.subttype is
    'This column is use for the document subtype for the transaction';


-- sqlcl_snapshot {"hash":"2fe4ddee5ee5e692424b28d6b5ca09be5ebb90f6","type":"COMMENT","name":"model_recycle_mas","schemaName":"orbrgs","sxml":""}